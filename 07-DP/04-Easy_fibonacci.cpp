// Previous  time complexity === O(2*N)

// Now with  dp  = O(n)
// Space = O(n)

class Solution {
public:

    // int solve(int n, vector <int>& dp){

    //     if (n <= 1){
    //         return n;
    //     }

    //     if (dp[n] != 0){
    //         return dp[n];
    //     }

    //     return dp[n]  = solve (n-1, dp) + solve(n-2, dp);
    // }
    
    int fib(int n) {
        // //  recursive solution 
        // // dp + memoaization 
        // if (n <= 1){
        //     return n;
        // }

        // vector <int> dp (n+1, 0);
        // return solve (n , dp);


        // linear  complexity  of  O(n)
        
        
    }
};